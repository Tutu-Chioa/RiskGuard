# 复制为 .env 并按本机路径修改（.env 不会被提交到 git）
# cp .env.example .env

# 媒体爬虫项目路径（必填则启用真实爬取，不填则使用模拟数据）
MEDIACRAWLER_PATH=/path/to/sys2/MediaCrawler

# ========== 生产环境必做（上线前请务必配置）==========
# 1) SECRET_KEY：生成并填入。在项目根目录运行：
#    python backend/scripts/generate_secret_key.py
#    将输出的字符串复制到下面，取消注释并替换。
# SECRET_KEY=粘贴上面命令输出的随机字符串

# 2) 避免清空数据库：生产启动时必须二选一（取消下面其中一行的注释），否则会 DROP 表并插入示例数据
# SKIP_DROP_TABLES=1
# 或
# FLASK_ENV=production

# 3) CORS：生产环境建议只允许你的前端域名（取消注释并改域名）；不设则允许所有（仅适合开发）
# CORS_ORIGINS=https://your-frontend-domain.com

# ---------- 忘记密码邮件（可选）----------
# 配置后忘记密码将发邮件且不再向前端返回 token
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@example.com
# 重置链接中的前端基地址（用户点击邮件里的链接会跳转至此）
# RESET_PASSWORD_BASE_URL=https://your-frontend-domain.com

# ---------- 可选 ----------
# MEDIACRAWLER_PLATFORM=xhs
# MEDIACRAWLER_TIMEOUT=360
# MEDIACRAWLER_MAX_COMMENTS=5
